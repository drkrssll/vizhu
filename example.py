import vizhu

# user_search takes (string, boolean)
# (string, _) should be the username you would like to search
# (_, boolean) tells whether or not to write the output to a file 
vizhu.user_search("drkrssll", False)
